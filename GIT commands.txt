$ git clone link
this will get a copy of repositry

step 1: $ git add .

step 2: $git commit -m "explain the changes made"
by this you display the short note what you changed

step 3: $ git status
this will display the origin/master

step 4: $ git push origin HEAD

$ls




$ pwd
displays preseent working directory

$ cd folderName/
used to enter the following directory

$ cd ..
to come back to previous directory

$ cd 
to go back to home

$ mkdir directoryName
to create a directory

$ ls 
to display the list of files/directory in current folder

$ git status
displays the weather any modifications made file or not

$ git diff
displays the differences in code line to line

$ git add filename
adds the changes made in filename

$ git commit -m
this will comit the changes and displays the changes or updates made
 
$ git push 
will update on server



 
